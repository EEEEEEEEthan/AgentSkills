---
name: code-optimize
description: 代码优化以减少维护成本，重点减少类成员。在代码审查、重构或优化代码时使用。遵循封装复用、内联单次调用方法、移除冗余逻辑等原则。
---

# 代码优化

**终极目标：** 减少维护成本。

**子目标：** 减少类成员；每个 `private` 成员都被引用至少 2 次。

不要为了拆而拆长方法；仅当片段可复用时才抽取。

生命周期与职责边界（附着物挂接 / 初始化 / 读档恢复）
[entries/lifecycle-and-single-responsibility.md](entries/lifecycle-and-single-responsibility.md)

C# field 属性、属性缓存等补充说明
[entries/csharp.md](entries/csharp.md)

## 优化检查清单

执行代码优化时，按以下清单逐项检查：

### 通用规则

- [ ] 移除冗余逻辑（未被调用过的逻辑）
- [ ] 封装，复用重复代码片段
- [ ] 仅被引用过一次的方法，应该成为内联实现或者成为闭包方法
- [ ] 对外优先保留单一入口（如 `MarkXxx` 同时表达设置与清除），避免并行维护 `SetXxx`/`ClearXxx` 双接口
- [ ] 事件处理器如果只被订阅一次，应该内联为 lambda 表达式以减少类成员
- [ ] 仅被一个方法调用过多次的方法，应该成为内部方法
- [ ] 高频方法里的内部方法应该尽量用 `static`
- [ ] 移除所有多余空行
- [ ] 旧的文档注释需要检查正确性。不要增加新的文档注释
- [ ] 检查所有元素（类、字段、属性、方法等）命名；局部命名直接改，`public` 元素提出更好方案后再改
- [ ] 布尔参数避免否定命名（如 `dontXxx`），改用肯定语义（如 `deferXxx`、`initializeImmediate`）
- [ ] 方法内不要读取全局单例判断「当前处于何种模式」；把 `Initialize` / `Restore` 等分支决策留给调用方
- [ ] 用 `bool` 区分互斥生命周期时，优先拆成独立方法（如 `Initialize` 与 `Restore`），并在文档中写明二者互斥、不可对同一挂接各调一次
- [ ] 变更类方法（如 `AddSkill`）只做一件事；需要副作用时由调用方在调用后显式 `handler.Initialize` / `handler.Restore`，不要通过参数把副作用塞回方法里
- [ ] 子类扩展基类生命周期时，在 `override` 里 `base.Xxx()` 后追加即可；避免再引入只被基类调一次的 `PostInitialize` / `PostRestore` 钩子
- [ ] 充分利用最新版本的语言特性去简化代码

### C# 特定规则

- [ ] 只读属性如果没有特殊逻辑（如计算、延迟初始化等），应该改为 `readonly` 字段以减少类成员
- [ ] 属性可以改为 `field` 关键字的要用 `field`（详见 [entries/csharp.md](entries/csharp.md)）
- [ ] C# 的 `const` 只能用于基本类型，如果只被引用过一次，应该内联到使用处（可以写在方法内部）
- [ ] 跨帧的状态应该封装为 `IEnumerator`，嵌套 `while` 表达子状态，用 `_process` 推进
- [ ] 完全信任 nullable 语法警告，不做任何多余的防御代码
- [ ] **优先使用现代 C# 特性简化代码**：
  - 如果可能，使用主构造函数 `(Type param)` 替代传统构造函数
  - 使用集合表达式 `= []` 替代 `new List<T>()`
  - 如果可能，使用 `foreach` 替代 `for` 循环（除非需要索引）
  - 使用 `??=` 和 `?.` 等空值合并操作符
- [ ] **避免过度复杂化**：
  - 不要添加不必要的中间变量或属性来跟踪状态
  - 如果属性本身已经缓存了值，不需要额外的缓存跟踪属性
  - 缓存失效逻辑应该尽可能简单直接
  - 移除所有未被使用的方法和属性
- [ ] **属性缓存（避免冗余 API/引擎调用）**（详见 [entries/csharp.md](entries/csharp.md)）

### GDScript 特定规则

- [ ] 仅被引用过一次的 `const`，应该内联到使用处（例外：如果 `const` 有缓存意义，如字典、集合等需要重复创建的数据结构，应保留）

# 维护此文档

检查清单留在本文件；一句话原则直接写在正文。仅案例、代码示例等实质内容放 `entries/`，不足一段的不单独建子文档。
